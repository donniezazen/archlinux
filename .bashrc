# ArchLinux
#alias pacs="sudo pacman -S"
#alias pacsyu="sudo pacman -Syu"
#alias pacss="pacman -Ss"
#alias pacr="sudo pacman -Runs"
#alias pacsyy="sudo pacman -Syy"
#alias pacqi="pacman -Qi"
#alias pacqs="pacman -Qs"
#alias pacsi="pacman -Si"
#alias aurs="pacaur -S --noedit"
#alias aursyu="pacaur -Syua --noedit"
#alias aurss="pacaur -Ssa"
#alias aurqi="pacaur -Qi"
#alias aurqs="pacaur -Qs"
#alias aursi="pacaur -Si"
#alias repoupdate="sudo reflector --verbose -l 200 -p http --sort rate --save /etc/pacman.d/mirrorlist"
#alias grub-update="sudo grub-mkconfig -o /boot/grub/grub.cfg"
#alias age="echo $(($(($(date +%s) - $(date -d "$(head -1 /var/log/pacman.log | cut -d ' ' -f 1,2 | tr -d '[]')" +%s))) / 86400)) days"
#alias winsteam="WINEDEBUG=-all optirun wine ~/.wine/drive_c/Program\ Files\ \(x86\)/Steam/Steam.exe -no-dwrite >/dev/null 2>&1 &"
#alias testcolors="/home/donnie/Downloads/Arch/colors5.sh"

#Command Not Found using pkgfile
#source /usr/share/doc/pkgfile/command-not-found.bash
